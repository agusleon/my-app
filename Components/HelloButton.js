import React from 'react';
import {Button} from 'react-native-paper';

const HelloButton = () => {
    <Button icon="rocket1" mode="contained" onPress={() => console.log('Pressed')}>
        Press me
    </Button>
}

export default HelloButton;